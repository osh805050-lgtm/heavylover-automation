# 비밀 정보 — 절대 커밋 금지
.env
.env.local
*.secret
*_token.json
gcp-service-account.json
*service-account*.json
*-credentials.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# 로그 / 런타임 산출물
logs/
*.log

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# 데이터 원본 (용량/개인정보)
data/raw/
data/meta_ads/raw/
proposals/knowledge/precedents/official/_raw/
proposals/knowledge/legacy/

# 사업계획서 제출 폴더 (개인 신분 서류 — 주민등록·통장사본·신용보고서 등 자동 복사 대상)
# Codex adversarial review 2026-05-10 — git push 시 GitHub 노출 위험 차단
사업/

# 산출물은 git 추적 — 2026-05-01 MCP 파일 손실 사고 후 정책 변경
# proposals/outputs/        → 추적 (gitignore 미차단)
# docs/analysis_10b/        → 추적 (gitignore 미차단)
# docs/strategy/outputs/    → 추적 (.gitkeep으로 폴더 보존)
# docs/expansion/outputs/   → 추적 (.gitkeep으로 폴더 보존)
# data/analysis_10b/sheets/ → 추적 (.gitkeep으로 폴더 보존)

# 엑셀 출력물 (수시 생성)
*.xlsx
!정부지원사업_레이더_*.xlsx

# 설치파일 / 대용량 바이너리
*.exe

# MCP 설정 (서비스 계정 경로 노출 방지)
.mcp.json
